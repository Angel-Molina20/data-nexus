import { CheckCircle2, Code2, FileJson2, Redo2, RotateCcw, Save, Undo2 } from "lucide-react";
import { Link } from "react-router";

import type { BuilderState } from "./state";

export function QueryBuilderHeader({ name, connection, state, canValidate, canCompile, busy, onSave, onValidate, onCompile, onUndo, onRedo, onReset }: { name: string; connection: string; state: BuilderState; canValidate: boolean; canCompile: boolean; busy: string | null; onSave: (validate: boolean) => void; onValidate: () => void; onCompile: () => void; onUndo: () => void; onRedo: () => void; onReset: () => void }) {
  return <header className="border-b bg-white px-4 py-3"><div className="flex flex-wrap items-center gap-3"><div className="mr-auto"><div className="flex items-center gap-2"><h1 className="text-lg font-bold">{name}</h1>{state.dirty ? <span className="rounded-full bg-amber-100 px-2 py-1 text-xs font-semibold text-amber-800">Cambios sin guardar</span> : null}{state.readOnly ? <span className="rounded-full bg-slate-100 px-2 py-1 text-xs">Solo lectura</span> : null}</div><p className="text-xs text-slate-500">{connection} · Revisión {state.revision} · AST {state.workingQuery.schema_version}</p></div><div className="flex flex-wrap gap-2"><button className="icon-button" disabled={state.readOnly || !state.history.length} title="Deshacer (Ctrl+Z)" onClick={onUndo}><Undo2 className="size-4" /></button><button className="icon-button" disabled={state.readOnly || !state.future.length} title="Rehacer (Ctrl+Shift+Z)" onClick={onRedo}><Redo2 className="size-4" /></button><button className="icon-button" disabled={state.readOnly || !state.dirty} title="Descartar cambios" onClick={onReset}><RotateCcw className="size-4" /></button><Link className="btn-secondary" to={`/queries/${state.queryId}/edit-json`}><FileJson2 className="size-4" />JSON</Link><button className="btn-secondary" disabled={state.readOnly || !state.dirty || Boolean(busy)} onClick={() => { onSave(false); }}><Save className="size-4" />Guardar</button><button className="btn-secondary" disabled={!canValidate || Boolean(busy)} onClick={onValidate}><CheckCircle2 className="size-4" />{busy === "validate" ? "Validando…" : "Validar"}</button><button className="btn-primary" disabled={!canCompile || Boolean(busy) || Boolean(state.validation && !state.validation.valid)} onClick={onCompile}><Code2 className="size-4" />{busy === "compile" ? "Compilando…" : "Compilar"}</button></div></div><p className="mt-2 text-xs text-slate-500">La ejecución estará disponible en la siguiente fase. Aquí solo se genera una vista previa parametrizada.</p></header>;
}
