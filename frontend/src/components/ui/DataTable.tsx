import type { ReactNode } from "react";
import { ErrorState, LoadingState } from "./FeedbackStates";
import { cx } from "./utils";
export interface DataColumn<Row> { align?: "left" | "center" | "right"; header: ReactNode; key: string; render: (row: Row) => ReactNode; }
interface DataTableProps<Row> { caption: string; columns: Array<DataColumn<Row>>; density?: "comfortable" | "compact"; empty?: ReactNode; error?: string; getRowKey: (row: Row) => string; loading?: boolean; rows: Row[]; stickyHeader?: boolean; }
export function DataTable<Row>({ caption, columns, density = "comfortable", empty, error, getRowKey, loading = false, rows, stickyHeader = false }: DataTableProps<Row>) { if (loading) return <LoadingState label={`Cargando ${caption.toLowerCase()}…`} />; if (error) return <ErrorState message={error} />; if (!rows.length) return <>{empty ?? <p className="p-6 text-center text-sm text-muted">No hay datos disponibles.</p>}</>; const padding = density === "compact" ? "px-3 py-2" : "px-4 py-3"; return <div className="max-w-full overflow-x-auto rounded-md border border-border"><table className="min-w-full border-collapse text-left text-sm"><caption className="sr-only">{caption}</caption><thead className={cx("bg-surface-muted", stickyHeader && "sticky top-0 z-10") }><tr>{columns.map((column) => <th className={cx("border-b border-border text-xs font-semibold uppercase tracking-wide text-muted", padding, column.align === "right" && "text-right", column.align === "center" && "text-center")} key={column.key} scope="col">{column.header}</th>)}</tr></thead><tbody>{rows.map((row) => <tr className="bg-surface hover:bg-surface-muted" key={getRowKey(row)}>{columns.map((column) => <td className={cx("border-b border-border text-foreground-secondary last:border-b-0", padding, column.align === "right" && "text-right", column.align === "center" && "text-center")} key={column.key}>{column.render(row)}</td>)}</tr>)}</tbody></table></div>; }
